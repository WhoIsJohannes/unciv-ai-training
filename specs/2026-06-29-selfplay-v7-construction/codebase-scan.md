# Deep Codebase Scan — selfplay-v7-construction (Step 7)

Exact integration mechanics for the v7 per-city construction head. Real anchors below.

## 1. Record mechanics (the two new VARIABLE blocks)
- **VARIABLE byte format** (Observation.kt:64-71): `writeBlock` emits `[u16 count]` (`b.u16(count)`, count = `values.size/perItem`) then `count×perItem` values in the block dtype. So a VARIABLE block = `[u16 ncities] + [ncities×perItem]`.
- `mask_construction` is declared `varU8("mask_construction", constrW, construction)` (Featurizer.kt:136) — perItem=constrW, count=ncities.
- **recordStep appends FIXED blocks** (DataPlaneHooks.kt:224-226): `obs.blocks + Block("actions",DT_F32,FIXED,0,actions) + Block(BLOCK_BEHAVIOR_LOGP,DT_F32,FIXED,0,behaviorLogp)`. **v7 appends two more**: `+ Block("construction_action",DT_F32,VARIABLE,perItem=1,cAct) + Block("construction_logp",DT_F32,VARIABLE,perItem=1,cLogp)` — count=ncities each.
- **`actions`/`behaviorLogp` are width `MASK_HEADS.size` = 4** (`MASK_HEADS = [tech, policy, greatPerson, diplomaticVote]`, SampleSchema.kt:133). chooseAndApply fills [0]=tech,[1]=policy; [2],[3] stay −1/0 (heuristic, unmodeled). **Construction is NOT a MASK_HEADS slot** — it is a separate per-entity VARIABLE block. (Resolves council #13: construction has its own block, not a fixed-index head.)
- **Schema bump**: `SampleSchema.VERSION 4→5` (Kotlin :27) + `schema.py SCHEMA_VERSION 4→5` (:20). Reader (`reader.py:_decode_blocks`) is descriptor-generic (`kind=var` → `<H` count then perItem items) → new f32 VARIABLE blocks decode free. Python gate `expect_compatible` raises if `version != SCHEMA_VERSION` (schema.py:38-45) → old v4 shards refuse. **Layout changed (new blocks) → the bump is required to perish v4 shards, not cosmetic** (resolves council #12).

## 2. Per-city decision loop (chooseAndApply)
- `handleCivTurn` (DataPlaneHooks.kt:102) builds `obs = state.featurizer.observe(civ)` then calls `chooseAndApply` (:107) → returns `Pair<FloatArray,FloatArray>` (actions[4], behaviorLogp[4]).
- **own_cities order WITH CAP** (Featurizer.kt:61): `val ownCityList = x.cities.sortedBy{it.id}.let{ if(it.size>caps.maxOwnCities){overflow=true; it.take(caps.maxOwnCities)} else it }`. **No puppet/razing filter** — full city set, capped at maxOwnCities. ⚠ The decision loop MUST reproduce the SAME capped+sorted list, or mask rows drift. **Design requirement: extract this into ONE shared function** (e.g. `Featurizer.orderedOwnCities(civ)`), reused by featurizer + decision loop. (Resolves council #2 + surprise.)
- mask row access: `obs.block("mask_construction")` returns flat `ncities×constrW` floats; city i's row = `copyOfRange(i*constrW,(i+1)*constrW)`. `boolMask` (DataPlaneHooks.kt:140) thresholds `!=0f`.
- Because obs and apply run in the SAME synchronous `handleCivTurn`, the city set cannot change between observation and application (no capture/destruction mid-call) — resolves the "capture between obs and apply" edge (council #7).

## 3. PolicyProvider + ONNX seam
- `PolicyProvider` interface (PolicyProvider.kt:14-38): `chooseIndex(head,civ,mask,turn)`, `chooseIndexWithLogp(head,civ,mask,turn): Pair<Int,Float>` (default: uniform-legal `ln(1/nLegal)`), `actUnit(unit)`. **Add `chooseConstructionWithLogp(civ, city, cityRow, legalMask, turn): Pair<Int,Float>`** (per-entity — needs city). Default impl = uniform-legal (mirror chooseIndexWithLogp). RandomPolicy (PolicyProvider.kt:45-56) draws `rngFor(civ,turn).nextInt(legal.size)`.
- `MaskedChoice.chooseWithLogp(logits,mask,eval,rng)` (MaskedChoice.kt:32-50): filter legal; stable log-softmax; **ONE `rng.nextDouble()*sum` draw** (sampling) or argmax (eval); returns `idx to (logits[idx]-maxL-ln(sum))`. **Reuse byte-identically for construction.**
- `OnnxPolicy.logitsFor` (OnnxPolicy.kt:115-125) memoized `Memo(key="game|civ|turn", techLogits, policyLogits)` in a `ThreadLocal`; rich path `forwardRich(obs, tileMap)`. **v7: extend Memo to hold per-city `constructionLogits: Array<FloatArray>` ([ncities][constrW]); ONE forward per (game,civ,turn) produces all rows; chooseConstructionWithLogp indexes row=cityRow.** OnnxContract (SampleSchema.kt:65) `OUTPUT_TECH="tech_logits"`, `OUTPUT_POLICY="policy_logits"` → add `OUTPUT_CONSTRUCTION="construction_logits"`. (Single inference → resolves council #8 O(N).)

## 4. Python net + export + train + dataset
- `RichPolicyValueNet.forward(inputs)` (model.py:79-113): `_TokenEncoder` per token type → `masked_pool` → `trunk` (256h) → `tech_head/policy_head/value_head`. `_TokenEncoder.forward` (model.py:66-77): `masked_pool(self.mlp(tokens), mask)` — **per-city embeddings `self.mlp(tokens)` [B,Ncities,token_dim] are POOLED AWAY**. v7: capture pre-pool `token_embeddings` for own_cities; `construction_head = MLP(token_embeddings ⊕ broadcast trunk h) → [B,Ncities,constrW]`. forward returns 4-tuple `(tech, policy, construction, value)`.
- `export_onnx._RichPolicyOnly.forward` (export_onnx.py:48-60): returns `(tech, policy)` (value dropped). `torch.onnx.export(... output_names=[OUTPUT_TECH,OUTPUT_POLICY], dynamic_axes=dyn)` (:163-171). v7: add `construction` to the wrapper return + `OUTPUT_CONSTRUCTION` to output_names + `dyn["construction_logits"]={0:"batch",1:"n_cities"}`.
- `train.py._masked_logp(logits,actions,mask)` (:28-35): masked log-softmax, gathers chosen, **0 where action<0**. `_optimize_actor_critic` (:115-): GAE snapshot (:163-179) `old_logp = stored_old_logp.detach() if not None else (_masked_logp(tech)+_masked_logp(policy)).detach()`; live logp (:208/:245) `= _masked_logp(tl,a_tech,m_tech)+_masked_logp(pl,a_policy,m_policy)`. **v7: add `+ _masked_logp(cl, a_construction, m_construction)` summed over cities to BOTH live logp and (at generation) the stored behavior old_logp.** `stored_old_logp` stays **[T] scalar** (sum over heads+cities) → ratio `(logp-old_logp)` stays per-step scalar (resolves council #1,#10). micro-batch `forward_chunk_fn(lo,hi)` slices uniformly → pad ragged Ncities to max BEFORE chunking.
- `dataset.py` (:196-202): reads `behavior_logp[0]`(tech)/`[1]`(policy); builds `TrainTrajectory`. v7: load `construction_action`/`construction_logp` VARIABLE blocks (ragged [T,Ncities_i]) + `mask_construction`; **pad to max Ncities** (`_pad_to_max`) so the trainer sees uniform [T,Nmax,·]; pad action=−1, logp=0, mask=0 (inert rows contribute 0 logp).

## 5. Tests + run driver + flag
- Parity: `FairnessAndDeterminismTests.maskParity_techMatchesEngine` (:183-192) asserts mask[idx]==`canBeResearched`. Extend: `maskParity_constructionMatchesEngine` (mask[idx]==`canConstruct`, buildings 0-indexed, units offset buildingCount). Determinism: `determinism_sameStateSameBytes` (:144-153) — extend so shards WITH construction blocks are byte-identical on replay.
- **Flag bridge is POSITIONAL** (SelfPlayRunner.kt gen() parses `args[1..7]`: model,out,n,maxTurns,threads,seed,mapSize). Add `args[8]=controlConstruction` → `SampleConfig.controlConstruction:Boolean`. run_loop.py: `ap.add_argument("--control-construction", ...)` → thread into the `gradle_selfplay(["gen",...,str(control).lower()])` arg list. (No named-flag parsing on JVM side.)
- **Vocab indexing trap**: `constructionCode(name)` (Vocab.kt:51-56) is **1-indexed, 0=none** (`building→b+1`, `unit→buildingCount+u+1`). But `constructionMask`/net logits are **0-indexed `constrW`-wide** (`m[building(idx)]`, `m[buildingCount+unit(idx)]`). **`constructionId(idx)` must invert the 0-indexed MASK space**: `idx<buildingCount → buildingName(idx); else → unitName(idx−buildingCount)`. Using the constructionCode (1-indexed) inverse would be off-by-one (silent building/unit mislabel). Add a round-trip assert in the parity test.

## Top surprises
1. **own_cities is CAPPED at `caps.maxOwnCities`** (Featurizer.kt:61) — the decision loop must reproduce the capped+sorted list, else recorded rows misalign with the mask. Centralize the ordering in one function.
2. **`constructionId` inverts the 0-indexed mask space, NOT 1-indexed `constructionCode`** — off-by-one trap; the legality/parity test (recorded idx → name → in-mask, recorded==applied) is the guard.
3. **Python→JVM flag bridge is positional args only** — `--control-construction` is a positional `args[8]`, threaded through `SampleConfig`, not a named JVM flag.
4. **Per-city embeddings are pooled away** in `_TokenEncoder` — must capture pre-pool `token_embeddings` for the construction head.
5. **`actions`/`behavior_logp` are width 4 (MASK_HEADS), not 2** — construction is a separate per-entity VARIABLE block; stored old_logp is a [T] scalar = sum over heads+cities; the importance ratio stays per-step scalar.
6. **No-op (OFF==v6) is a zero-summand proof**: OFF records zero construction decisions → `Σ_cities _masked_logp(construction)≡0` → loss/gradient identical to v6; the construction head gets zero gradient (inert). Test = OFF construction summand is 0 + OFF curve matches v6.
7. **Ragged Ncities must be padded to max before micro-batch chunking** (train.py forward_chunk_fn slices on step index) — pad action=−1/logp=0/mask=0 so inert rows contribute 0.
8. **u16 count caps VARIABLE blocks at 65535 items** — safe (maxOwnCities≪65535) but brittle if maxOwnCities ever grows; comment it.
